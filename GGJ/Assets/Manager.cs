using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace UnityStandardAssets.Characters.ThirdPerson
{

    public class Manager : MonoBehaviour
    {
        /*
    public GameObject[] player;
    
    public static string type = "player";
        public static int playerEnabled = 0;

    // Start is called before the first frame update
    void Start()
    {
            player[0].GetComponent<ThirdPersonCharacter>().enabled = false;
            player[1].GetComponent<ThirdPersonCharacter>().enabled = false;
            player[2].GetComponent<ThirdPersonCharacter>().enabled = false;
            player[3].GetComponent<ThirdPersonCharacter>().enabled = false;

        }

        // Update is called once per frame
        void Update()
    {

        if (Input.GetKeyDown(KeyCode.Z))
        {
            CameraFollow.target = player[0].transform;
            player[0].GetComponent<ThirdPersonCharacter>().enabled = false;
        }
        else if (Input.GetKeyDown(KeyCode.X))
        {
            CameraFollow.target = player[1].transform;
            player[1].GetComponent<ThirdPersonCharacter>().enabled = false;

            }
            else if(Input.GetKeyDown(KeyCode.C))
        {
            CameraFollow.target = player[2].transform;
            player[2].GetComponent<ThirdPersonCharacter>().enabled = false;
            }
            else if(Input.GetKeyDown(KeyCode.V))
        {
            CameraFollow.target = player[3].transform;
            player[3].GetComponent<ThirdPersonCharacter>().enabled = false;
            }
        }
}
*/
    }
}